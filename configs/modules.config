/*
params {
    modules {
        'jq' {
            args = '-r'
        }
    }
}
*/

process {
    withName: 'JQ' {
        // -r : Print raw output
        ext.args = '-r'
    }
    withName: 'PRANK' {
        ext.args  = '-codon -once -F'
        ext.args2 = '-convert -f=paml -keep'
    }
}